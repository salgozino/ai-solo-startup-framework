// Package main is the composition root for the company agent framework CLI.
// This stub ensures go build ./... compiles from day 1.
// Full implementation arrives in PR 8 (composition root + E2E loop).
package main

func main() {}
