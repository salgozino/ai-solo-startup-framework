tenant: acme
auth_token_env: A2A_AUTH_TOKEN
agents:
  - name: ceo
    role: ceo
    provider: claude-code
gateways:
  telegram:
    recipient_env: TELEGRAM_OWNER_ID
risk_policy:
  telegram_send:
    risk: risky
    allowed_roles: [ceo]
