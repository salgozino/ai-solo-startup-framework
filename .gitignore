# Agent tooling caches — machine-specific, contain absolute local paths
.atl/
.codegraph/

# Local agent permission overrides — machine-specific, not a team convention
.opencode/

# Pi agent workspace (gentle-shell migration) — local extension config, stays
# on the machine and never reaches the remote
.pi/

# Git worktrees
.worktrees/

# Go build output
/company
*.exe
*.test
*.out

# Local environment — gateway tokens are read from the environment, never committed
.env
.env.*
!.env.example

# Local configuration
company*.yaml
sdd/
